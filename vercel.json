{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cp prisma/schema.mongodb.prisma prisma/schema.prisma && npx prisma generate && next build",
  "installCommand": "npm ci",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["bom1"],
  "env": {
    "DATABASE_URL": "@database_url"
  }
}
